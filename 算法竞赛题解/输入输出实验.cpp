#include<iostream>
using namespace std;
int main2() {
	int a, b;
	scanf_s("%d%d", &a, &b);
	printf("%d %d\n", a, b);
	return { 0 };
	//实验B1 达到预期结果 用空格分隔
	//实验B2 达到预期结果 不同两行输入
	//实验B3 达到预期结果 中间隔开用tab或空格或换行符
	/*实验B4  用s代替2 重复实验B1 未达到预期结果	返回-858993640
					   重复实验B2 未达到预期结果    同上返回
					   重复实验B3 未达到预期结果    同上返回

	*/
	
}